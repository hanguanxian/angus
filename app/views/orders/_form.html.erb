<table>
  <%= render(@cart.line_items) %>

  <tr class="total_line">
    <td colspan="2">合计</td>
    <td class="total_cell"><%= number_to_currency(@cart.total_price , :unit=>"￥") %></td>
  </tr>
</table>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Addr</th>
      <th>Phone</th>
      <th>Defau</th>
      <th>Custom</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @details.each do |detail| %>
      <tr>
          <td><%= detail.name %></td>
          <td><%= detail.addr %></td>
          <td><%= detail.contach %></td>
          <%= form_for(@order) do |f| %>
          <input name="name" type="hidden" value="<%= detail.name %>"/>
          <input name="address" type="hidden" value="<%= detail.addr %>"/>
          <input name="contach" type="hidden" value="<%= detail.contach %>"/>
          <td><button type="submit" class="btn btn-default" name="submit">确认</button></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to '新增收货地址', new_detail_path %>
<%= link_to '返回', products_path, class:"pull-left btn btn-default" %>
